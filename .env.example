# === Identite conteneur ===
PUID=1000
PGID=1000
TZ=Europe/Paris

# === Acces au bureau (auth webtop, servie sous HTTPS) ===
DESKTOP_USER=jerome
DESKTOP_PASSWORD=change-moi

# === VPS / Traefik (deploiement streame uniquement) ===
# Le sous-domaine doit pointer (DNS A) vers l'IP du VPS ou tourne Traefik.
DESKTOP_DOMAIN=desktop.mobang.fr